const container = {
  bgcolor: "background.default",
  p: 1.5,
  borderRadius: 3,
  maxWidth: "700px",
}

const rightIcon = {
  mr: 1.5,
}

const leftIcon = {
  ml: 1.5,
}

const inputStyles = {
  container,
  rightIcon,
  leftIcon,
}

export default inputStyles
