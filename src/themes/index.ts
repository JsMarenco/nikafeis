/* eslint-disable no-unused-vars */
import darkTheme from "./dark"
import lightTheme from "./light"

/**
 * All theme app themes names
 */
enum appThemes {
  dark = "dark_theme",
  light = "light_theme",
}

export { appThemes, darkTheme, lightTheme }
